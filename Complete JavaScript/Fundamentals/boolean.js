// js boolean object and boolean primitive type.

// javascript boolean primitive type 

// if boolean boject sent non empty it returns true otherwise false 
let a = Boolean("hi");
console.log(a);

a = Boolean();
console.log(a);